# requirements_server.txt
# ========================
# Minimal dependencies for the edge computing WebSocket server.
# The smart_vision_assistant/ core dependencies (ultralytics, opencv, etc.)
# must also be installed. See smart_vision_assistant/requirements.txt

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# CV core (from smart_vision_assistant — listed here for convenience)
opencv-python>=4.8.0
ultralytics>=8.1.0
numpy>=1.24.0
psutil>=5.9.0

# Optional: for .env support
python-dotenv>=1.0.0
