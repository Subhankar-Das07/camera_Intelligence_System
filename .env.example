# Copy to .env and fill in values. Do not commit .env.

# -- SMTP (Gmail app password; same names as Zerodha Kite backend) -------------
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
USERNAME_EMAIL=
PASSWORD_EMAIL=
EMAIL_FROM=

# Optional WhatsApp Cloud API
# WHATSAPP_TOKEN=
# WHATSAPP_PHONE_NUMBER_ID=
# WHATSAPP_DEFAULT_TO=
